# PocketBase migrations

Slopstack creates its initial core collections during bootstrap so a newly cloned
template can start without a migration step. Add subsequent schema changes here
using PocketBase's generated migration format; never edit an applied migration.
